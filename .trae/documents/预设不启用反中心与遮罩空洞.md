## 目标
- 当选择“沉浸柔光”“爆火高亮”“文物写实”预设时，不自动勾选“反中心力”和“遮罩空洞”。

## 修改点
- 在 `src/App.tsx` 的 `applyPreset` 中，将三个分支的 `setAntiCenterEnabled(true)` 和 `setVoidMaskEnabled(true)` 均改为 `false`，保留其他参数不变。

## 验证
- 选择任一预设后，右侧面板的两个复选框保持未勾选。
- 场景效果根据预设的其他参数变化，但不会出现中心洞。