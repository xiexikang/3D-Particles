## 原因定位
- 动力学排斥：`src/components/ParticleScene.tsx:339-349` 按 `antiCenterRadius` 与 `antiCenterStrength` 将半径内粒子径向推开，呈现中心低密度与环状亮度。
- 着色器挖孔（可选）：`src/components/ParticleScene.tsx:252-273` 使用 `uVoidRadius`/`uVoidSoftness` 与 `discard` 实现中心遮罩；默认 `voidMaskEnabled=false`，预设会打开。
- 默认值来源：`src/components/ParticleScene.tsx:32`（props 默认 true/半径 8/强度 0.5）、`src/App.tsx:13-15`（初始 state 同步为开启）。

## 调整目标
- 默认初始化时，避免中心出现空洞或明显圆环；保持其它交互与预设可用。

## 修改方案
1. 关闭默认反中心斥力：
   - 在 `src/App.tsx:13` 将 `useState(true)` 改为 `useState(false)`，即默认 `antiCenterEnabled=false`。
   - 在 `src/components/ParticleScene.tsx:32` 将 `antiCenterEnabled = true` 改为 `false` 以保障组件级默认一致。
2. 温和参数备选（如仍需轻微中心疏散而非空洞）：
   - 将 `antiCenterRadius` 初始化设为 `4`，`antiCenterStrength` 设为 `0.2`（`src/App.tsx:14-15`）。
3. 确保遮罩默认关闭：
   - 保持 `voidMaskEnabled=false`（`src/App.tsx:22`、`src/components/ParticleScene.tsx:32`），避免预设之外出现硬挖孔。

## 验证步骤
- 启动后观察随机云海模式：中心应填充，无明显圆环或空洞。
- 切换 TunePanel：手动开启“反中心斥力”或“遮罩”可重现圆环/空洞，验证开关生效。
- 预设切换：预设仍可打开遮罩效果，确认默认关闭不影响预设逻辑。

## 影响评估
- 仅变更默认参数，不改动运行时算法与交互；风险低。
- 性能与视觉的其它部分不受影响。