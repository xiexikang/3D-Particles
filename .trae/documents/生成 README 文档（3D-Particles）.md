# 项目 README 规划（不包含代码路径）

## 文档目标
- 生成一份中文 README.md，完整介绍项目用途、特性、技术栈、安装与运行方式、脚本说明、使用指南与配置说明。
- 严格不包含任何代码文件路径或系统绝对路径，仅保留项目名称与通用命令。

## README 目录
- 项目简介（数字展厅·粒子化粒子系统）
- 特性亮点（模式、特效、交互、性能优化）
- 技术栈与主要依赖（React、TypeScript、Vite、three、R3F、drei、postprocessing）
- 环境要求（Node.js >= 18）
- 快速开始（安装依赖、开发启动、构建、预览）
- NPM 脚本说明（dev/build/preview）
- 项目结构（仅用文字概述模块职责，不列出路径）
- 使用说明（图片/文本/GLTF 模式；参数调节；截图与视角重置）
- 配置说明（端口、HMR、严格 TS 等的文字说明，不列出具体文件）
- 资源与预设（说明存在示例图片与模型预设，不列路径）
- 许可与致谢（占位说明，后续可补齐）

## 关键信息（以文字形式写入，不含路径）
- 入口为单页应用，使用 React 进行渲染，支持四种模式（随机、文本、图片、GLTF）与多种特效（爆裂、涡旋、冲击波、黑洞、瀑布、彩虹、风场、呼吸、波纹、球壳、网格阵列）。
- 使用 three 与 @react-three/fiber 实现三维粒子渲染，@react-three/drei 提供交互控件，后期采用 Bloom。
- 提供上传/拖拽图片与 GLB 模型、文字输入的 UI 面板；另有参数面板用于调节粒子大小、Bloom 阈值与强度、反中心斥力、遮罩空洞、运动速度、相机角度、粒子数量等。
- 内置截图保存与视角重置功能；显示实时 FPS。
- 开发服务器默认监听本地端口（文档中以文字描述，不写具体端口号），支持热更新。

## 快速开始（命令）
- 安装依赖：`npm install`
- 开发启动：`npm run dev`
- 生产构建：`npm run build`
- 本地预览构建产物：`npm run preview`

## 执行步骤
1. 在项目根目录创建 `README.md`，写入上述内容（不包含任何代码文件路径或系统绝对路径）。
2. 提供“截图”与“许可”占位说明，后续可补充。
3. 保留常用命令与技术栈说明，确保新人可快速运行与理解。
4. 不修改现有代码，仅新增文档。

## 确认请求
- 请确认上述规划与“禁止包含路径”的约束是否满足您的要求。确认后我将生成并保存 `README.md`。