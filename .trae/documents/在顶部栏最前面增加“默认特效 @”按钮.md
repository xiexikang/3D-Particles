## 目标
- 在顶部栏最前面新增一个按钮，标签为“@”，用于快速恢复到默认效果（不叠加任何力场特效）。

## 行为
- 点击“@”将 `effect` 设置为 `'none'`，并关闭 `exploded=false`，不改变当前 `mode`（随机/文本/图片/GLTF）。

## 实施
- 在 `src/App.tsx` 的顶栏按钮组里，作为首个按钮插入“@”，调用现有 `setEffect('none')` 与 `setExploded(false)`。

## 验证
- 点击“@”后，场景仅保留基础噪声与目标吸引，不叠加旋涡、冲击波、黑洞等；终端无报错，预览切换正常。