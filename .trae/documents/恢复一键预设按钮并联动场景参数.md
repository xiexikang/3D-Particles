## 目标
- 在参数面板恢复“沉浸柔光 / 爆火高亮 / 文物写实”一键预设按钮，点击即批量更新参数并联动场景。
- 中心圆圈大小保持通过“反中心斥力”开关 + 半径/强度滑块调节。

## 实施
- 修改参数面板组件：增加三枚预设按钮与 `onApplyPreset` 回调。
- 修改应用层：实现 `applyPreset(name)`（设置 antiCenterRadius/Strength、bloomThreshold/Intensity、pointSize），并将回调传入参数面板。
- 场景层无需改动，继续使用传入的参数实时生效。

## 验证
- 点击三种预设，风格即时切换；滑动“中心圆圈半径/强度”圈大小即时变化；上传图片/GLB联动正常。