## 目标
- 增加风格预设管理：保存/应用/删除/导入/导出当前参数（圆圈半径与强度、Bloom 阈值与强度、粒子尺寸）。
- 增强可视化调试：增加 FPS/粒子数量调节与一键截图，便于快速评估与出图。

## 实施内容
- 预设管理
  - 在参数面板新增“保存为预设”“预设列表（应用/删除）”与“导入/导出”按钮。
  - 预设结构：{ name, antiCenterEnabled, antiCenterRadius, antiCenterStrength, bloomThreshold, bloomIntensity, pointSize }。
  - 本地持久化（localStorage），支持导出为 JSON 与从 JSON 导入。
- 调试能力
  - 添加 FPS 显示（每秒平均更新）。
  - 粒子数量滑块（范围例如 10k–100k），重新初始化粒子缓冲并保持交互逻辑不变。
  - 截图按钮：保存当前画面为 PNG。

## 验证
- 保存多个预设并在列表中切换，参数即时生效。
- 调整粒子数量后性能预估合理（桌面 >= 60 FPS，移动端按需降低）。
- 截图保存正确，FPS 读数与页面交互正常。

## 影响范围
- 只改动 UI 与状态管理，不改变核心粒子求解与采样逻辑；风险低，易回滚。

确认后我将实现预设管理与调试能力，并联动现有参数面板。