## 修改目标
- 仅移除顶部栏中的“开启麦克风驱动”按钮，不触及其他功能。

## 具体改动
- 在 `src/App.tsx` 的工具栏中删除该按钮：
  - 位置：`src/App.tsx:85`
  - 代码：`<button className="btn" onClick={() => setMicOn(v => !v)}>{micOn ? '关闭麦克风' : '开启麦克风驱动'}</button>`
- 保留 `micOn` 状态与传参以避免连锁修改；其默认值为 `false`，不会触发麦克风权限请求。

## 可选后续清理（需另行确认）
- 删除 `micOn` 相关状态与 props（`src/App.tsx:12`, `src/App.tsx:116` 以及 `src/components/ParticleScene.tsx:14, 35, 78-88`），彻底移除麦克风驱动逻辑。

## 验证
- 顶部栏不再显示麦克风按钮。
- 页面运行时不会弹出麦克风权限请求。