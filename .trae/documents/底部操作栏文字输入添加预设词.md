## 位置与现状
- 底部操作栏组件：`src/components/UploadPanel.tsx:69-144`
- 文字输入区：`src/components/UploadPanel.tsx:133-137`，通过 `submitText()` 调用父级 `onTextSubmit`
- 顶层接收：`src/App.tsx:129` 将 `onTextSubmit` 设置为 `setText(t); setMode('text'); setEffect('none')`
- 可复用样式：`src/styles.css:195-218` 的 `.preset-list` 与 `.chip`

## 目标
- 在“文字输入”区增加“预设文字”快捷选项，内容为：`美丽中国`、`世界和平`、`羊城广州`、`开心`、`快乐`
- 点击预设后，自动应用到场景（与“应用文字”按钮效果一致）

## 技术实现
- 在 `UploadPanel.tsx` 顶部新增常量：`TEXT_PRESETS = ['美丽中国','世界和平','羊城广州','开心','快乐']`
- 在“文字输入”区（`src/components/UploadPanel.tsx:133-137`）下方新增一块：
  - 结构：`<div className="section-title">预设文字</div>` + `<div className="preset-list">{TEXT_PRESETS.map(...)</div>`
  - 交互：`onClick` 时将 `textInputRef.current.value = preset`，随后调用 `submitText()`，保证沿用现有 trim 与数据流
- 样式复用：使用已有 `.preset-list` 与 `.chip`，无需新增 CSS

## 行为与兼容性
- 保留原有输入框与“应用文字”按钮；预设点击为快速应用
- 不影响图片/GLB上传逻辑；不改变 `App` 的状态流转

## 验证
- 点击每个预设后，`App` 切换到 `mode='text'` 并更新 `text`，可在场景中看到文字变更
- 回归测试：手动输入后回车或点击“应用文字”仍正常

## 变更文件
- `src/components/UploadPanel.tsx`（新增常量与 JSX 片段）
- 无需修改 `src/styles.css` 或其它文件