## 改动内容
- 文本模式：把像素到世界坐标的缩放常量从 `40` 提升为 `80`
  - 修改 `src/components/ParticleScene.tsx:112`、`:113`
- 图片模式（主线程）：同样把缩放常量从 `40` 提升为 `80`
  - 修改 `src/components/ParticleScene.tsx:218`、`:219`
- 图片模式（Web Worker）：保持与主线程一致，从 `40` 提升为 `80`
  - 修改 `src/workers/imageSampler.ts:35`、`:36`

## 不改项
- 相机参数（`position=[0,0,80]`、`fov=60`）暂不改。
- 随机云海/目标范围与物理参数不变，避免牵连其他效果。

## 预期效果
- 文本与图片模式的初始化展示区域整体放大约 2 倍，随机模式保持原样。
- 视觉更“填满”，轮廓更突出；性能基本不变（粒子数量与点大小未变）。

## 验证步骤
- 启动后分别切换到“文本”“图片”“随机”模式观察：文本/图片放大到约 80×80 的世界范围，随机仍为 ~60 区域。
- 检查 FPS 是否稳定；如出现过度绘制，可后续微调点大小或粒子数。