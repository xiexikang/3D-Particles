## 目标
- 让 UploadPanel 的“采样阈值”“亮度增强”滑杆与 TunePanel 使用一致的样式与交互：同样的容器类名、标签和数值显示方式。

## 具体改动
- 文件：`src/components/UploadPanel.tsx`
  - 将两处滑杆区域从 `control-row + section-title + input` 改为与 TunePanel 相同的 `div.grid` 容器结构。
  - 标签与数值显示用单个 `span`（例如：`采样阈值 0.40`、`亮度增强 0.90`），紧随其后为 `input type="range"`。
  - 保留现有最小/最大/步长设置，确保功能不变，仅统一样式与交互。

## 验证
- 打开应用，检查“图片上传”中的两个滑杆与“参数调节”区域滑杆在布局、交互反馈上保持一致（同样的 hover/宽度/间距）。
- 滑动后数值实时更新，采样与颜色效果正常。

## 范围
- 仅修改 `src/components/UploadPanel.tsx` 对应滑杆区域结构，其他文件不变。